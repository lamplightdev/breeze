{
  "name": "begin-app",
  "version": "1.0.1",
  "description": "Begin basic Hello World! app for Deno",
  "scripts": {
    "start": "run-s cross-env",
    "cross-env": "cross-env NODE_ENV=testing sandbox",
    "localbuild": "deno bundle --unstable --no-check src/http/get-test/index.src.js src/http/get-test/index.js"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}

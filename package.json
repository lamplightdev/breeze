{
  "name": "begin-app",
  "version": "1.0.0",
  "description": "Begin basic Hello World! app for Deno",
  "scripts": {
    "start": "run-s cross-env",
    "cross-env": "cross-env NODE_ENV=testing sandbox",
    "build": "deno bundle src/http/get-index/index.js src/http/get-index/index.js"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}

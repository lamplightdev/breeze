{
  "name": "begin-app",
  "version": "1.0.1",
  "description": "Begin basic Hello World! app for Deno",
  "scripts": {
    "start": "run-s cross-env",
    "cross-env": "cross-env NODE_ENV=testing sandbox",
    "localbuild": "rollup -c",
    "localbuild:watch": "rollup -c -w"
  },
  "devDependencies": {
    "@architect/sandbox": "latest",
    "cross-env": "^7.0.3",
    "rollup": "^2.56.3",
    "rollup-plugin-url-resolve": "^0.2.0"
  },
  "dependencies": {
    "@architect/functions": "^4.0.0",
    "npm-run-all": "^4.1.5"
  }
}
